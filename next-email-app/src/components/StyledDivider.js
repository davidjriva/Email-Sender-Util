import { Divider } from "@mui/material";

const StyledDivider = () => {
  return (
    <Divider
      sx={{
        backgroundColor: "#d3d3d3",
        height: "1px",
        margin: "16px 0",
        width: "100%",
      }}
    />
  );
};

export default StyledDivider;